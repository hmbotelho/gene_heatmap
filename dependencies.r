if (!require("shiny")) {
    install.packages("shiny")
    library(shiny)
}


if (!require("gplots")) {
    install.packages("gplots")
    library(gplots)
}

if (!require("colorspace")) {
    install.packages("colorspace")
    library(colorspace)
}